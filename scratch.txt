// 型パラメータTをクラスパラメータと同じような箇所に書くことができる？
  class Point(type T, val x: T, y: T)
// そもそも型パラメータとクラスパラメータに区別がない世界？(*はkind)
  class Point(val T: *, val x: T, y: T)
  // その場合、
  class Arrow(val F: * -> *, val x: F)
  でFは型コンストラクタを受け取ることになる…

// その他色々考えたことを書きなぐる欄